[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
autopep8 = "~=1.5.5"
black = "==20.8b1"
coverage = "~=5.4"
flake8 = "~=3.8.4"
importlib-metadata = {version = "~=3.4.0",markers = "python_version < '3.8'"}
isort = "~=5.7.0"
mypy = "~=0.800"
pylint = "~=2.6.0"
typed-ast = "~=1.4.2"  # Required by black, but Pipenv doesn't pull it in correctly on 3.8
# Required by importlib-metadata, but Pipenv doesn't pull it in correctly
zipp = {version = "~=3.4.0", markers = "python_version < '3.8'"}

[packages]
asyncssh = "~=2.5.0"
websockets = "~=8.1"
